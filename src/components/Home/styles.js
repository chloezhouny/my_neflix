import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  wrapper: {
    width: '90vw',
    marginTop: '-9vw',
    // maxWidth: '840px',
    minHeight: '100vh',
    margin: 'auto',
    display: 'flex',
    flexDirection: 'column',
  },
}));
